server:
  port: 10306

spring:
  profiles:
    active: local

mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: net.hwyz.iov.cloud.tsp.vmd.service.infrastructure.repository.po